import { describe, it } from "node:test";
import { strict, strictEqual } from "node:assert";

describe("linked-pr", () => {
  it("should return a linked PR", async () => {
    // TODO: Write tests
    await import("./index.js");
  });
});
